

const StepTitle = ({ title }) => {
  return (
    <h2 className="text-[#0A0D14] font-inter text-sm md:text-base font-semibold leading-relaxed">
      {title}
    </h2>
  );
};
export default StepTitle;
